# 数据库管理软件

https://dbxio.com/cn

# 低配置大模型运行工具llama.cpp

https://llama.app/

命令文档：https://github.com/ggml-org/llama.cpp/blob/master/tools/cli/README.md

下载地址： https://github.com/ggml-org/llama.cpp/releases

中文社区：https://github.com/LlamaChinese/Llama-Chinese

参考https://macin.top/posts/85e84700/

```sh
# 下载并运行一个来自 Hugging Face 的小模型
llama cli -hf ggml-org/Qwen3.5-0.8B-GGUF

# 运行一个本地 GGUF 模型文件
llama cli -m ./models/llama-2-7b.Q4_K_M.gguf -p "你好，请问你是谁？"

# 启动服务
llama serve -m ./models/llama-2-7b.Q4_K_M.gguf
# 服务默认运行在 http://localhost:8080

```

设置环境变量

```ini
HF_ENDPOINT=https://hf-mirror.com
```

支持断点续传

```sh
pip install -U huggingface_hub

huggingface-cli download Qwen/Qwen3-8B-GGUF qwen3-8b-q4_k_m.gguf --local-dir .

hf download nullsumlabs/qwen3.8-9b-coder Qwen3.8-9B-Coder-Q4_K_M.gguf mmproj-Qwen3.8-9B-Coder-BF16.gguf --local-dir ./models

# 运行多模态
llama-server \
  -m Qwen3.8-9B-Coder-Q4_K_M.gguf \
  --mmproj mmproj-Qwen3.8-9B-Coder-BF16.gguf \
  -c 16384 \
  --jinja
```

下载模型,模型保存在`用户\.cache\huggingface\hub`，将其复制到llama.cpp的目录下，方便运行模型地址

```sh
llama cli -hf unsloth/Qwen3.5-4B-GGUF
```

运行服务

```sh
llama serve -m ./models/Qwen3.5-4B-Q4_K_M.gguf --port 8123 --reasoning off
llama serve -m ./models/Qwopus3.5-9B-coder-Exp-Q4_K_M.gguf --port 8123 --reasoning off

llama serve -m ./models/qwen3.5-4B-super-coder.Q4_0.gguf --port 8123 --reasoning off
```

关闭思考回答问题

```sh
llama cli -m ./models/Qwen3.5-4B-Q4_K_M.gguf --reasoning off --offline

llama cli -m ./models/Qwopus3.5-9B-coder-Exp-Q4_K_M.gguf  --reasoning off

我叫小明，今年99岁，请问我是多少年出生的？
```

读取文本文件

```sh
/read C:/Users/76493/Desktop/test.txt
```

环境变量设置缓存地址

```ini
LLAMA_ARG_SLOT_SAVE_PATH=D:\softwares\llama-b10883-bin-win-cpu-x64\data
```

对话命令说明

```sh
 /exit or Ctrl+C     stop or exit
  /regen              regenerate the last response
  /clear              clear the chat history
  /read <file>        add a text file
  /glob <pattern>     add text files using globbing pattern
```

RAG增强搜索知识库

https://hf-mirror.com/nomic-ai/nomic-embed-text-v2-moe-GGUF/tree/main

蒸餾版本，浓缩大模型智慧
https://hf-mirror.com/empero-ai/Qwen3.8-4B-Distill-GGUF/tree/main

# skills安装与使用

skills库
https://www.skillhub.cn/skills?category=content-creation&sortBy=score

https://skillsmp.com/zh

安装skill库

```sh
 npm install -g @skill-hub/cli
```

将skills下载到文件夹

```sh
skillhub install laoxi/fanqiexiaoshuoxiezuo --dir "D:\novel\xiaoshuo\skills"
```

```sh
npx skills add <GitHub仓库地址>
```

或者，下载skills的zip包解压放到当前项目解压到`.opencode/skills/<技能名>/SKILL.md`
全局使用skills的话，`用户/.config/opencode/skills/<技能名>/SKILL.md`

https://www.skillhub.cn/skills/davtime/xiaoshuopingfen
安装提示词

```
请根据 https://skillhub.cn/install/skillhub.md，安装 @davtime/xiaoshuopingfen。
```

SKILL.md编写规范

```
---
name: test-skill 名称
description: 描述，主要作用。
---

# 具体的指令内容

在这里编写详细的执行步骤、输出格式要求或检查清单...
```

opencode中调用`@skill-name`，如`@test-skill`，或者`/技能名`

项目根目录下配置opencode.json

## skills目录

https://xianyu110.github.io/ai-coding-tutorial/#05-skills

- **SKILL.md** 就是食谱本身（菜名、步骤、注意事项）
- **scripts/** 就是配套的厨房小工具（削皮刀、量杯 —— 封装好的辅助脚本）
- **resources/** 就是附赠的食材包和调料配比表（模板、示例数据、配置）
- **references/** 就是食谱末尾的"参考书目"（营养学标准、食品安全规范 —— AI 可随时查阅的参考资料）

```txt
skill-xxx/               # Skill 根目录（命名规范：小写+短横线）
├── SKILL.md              # 核心：技能描述文件（必选）
├── scripts/              # 辅助脚本目录（可选）
│   ├── helper.py          # Python 辅助脚本
│   └── utils.js           # JavaScript 工具函数
├── resources/            # 配套资源目录（可选）
│   ├── template/          # 模板文件（如代码模板、报告模板）
│   ├── examples/          # 示例文件（如输入/输出示例数据）
│   └── config/            # 配置文件（如规则定义、默认参数）
├── references/            # 参考文档目录（可选）
│   ├── best-practices.md    # 最佳实践文档
│   ├── api-docs.md         # API 参考文档
│   └── standards.md        # 行业/团队编码规范
└── requirements.txt        # 依赖声明（可选，列出脚本需要的第三方包）
```

```md
---
# 元数据（Frontmatter，YAML 格式）
name: react-component-generator # 技能名称（唯一标识）
version: 1.0 # 技能版本
description: 根据需求生成符合项目规范的 React 组件文件集 # 技能简介
trigger: ["创建组件", "新建React组件", "生成组件"] # 触发关键词
tools: ["typescript", "react"] # 依赖工具
author: your-name # 技能作者
---

# React 组件生成器

## 执行步骤

1. 确认组件名称和功能需求
2. 在 src/components/{componentName}/ 目录下创建文件
3. 按照 resources/template/ 中的模板生成代码
4. 运行 scripts/validate.js 验证组件结构

## 输出规范

- 所有文件创建完成后，报告创建的文件列表
- 给出组件的使用示例代码

## 错误处理

- 如果目录已存在，提示用户确认是否覆盖
- 如果缺少依赖包，提示安装命令

## 示例

给一个完整的输入→输出示例。
```

## 安装

```sh
# 安装 Anthropic 官方全部 Skill（全局安装）
$ npx skills add anthropics/skills -g

# 只安装指定 Skill（推荐按需安装）
$ npx skills add anthropics/skills@frontend-design -g
```

## Skill库

https://github.com/vercel-labs/skills
https://github.com/anthropics/skills

ComposioHQ/awesome-claude-skills 127+ 10大分类，含59个SaaS应用集成Skill
alirezarezvani/claude-skills 235+ 9大领域，含25个POWERFUL级高级Skill
travisvn/awesome-claude-skills 持续更新 精选列表，社区投票排名
glebis/claude-skills 专项 专注特定工作流的高质量Skill
bytedance/agentkit-samples

# 向量数据库chroma

https://chroma.org.cn/

安装chromadb

```sh
pip install chromadb
```

# AI Coding

https://xianyu110.github.io/ai-coding-tutorial/

# 代码审查

```md
你是一名资深工程师，请对以下代码进行审查：
模块/功能: [模块名称]
目标/预期行为: [描述预期功能]
当前问题: [Bug或异常表现]
代码如下: [粘贴代码]
请从以下角度审查：

1. 安全性：检查漏洞、硬编码密钥、敏感信息泄露
2. 性能：提出优化建议
3. 可维护性：识别代码异味、冗余或复杂度过高
4. 错误处理：发现未处理异常或潜在Bug
5. 一致性：变量命名、方法命名、导入顺序等
6. 测试覆盖：列出应补充的测试用例
   请按严重程度排序，提供文件/行号级别的具体改进建议。

你是一名资深工程师，
阅读app\src\main\java\com\xld\noplayapp的代码，
请从以下角度进行代码审查：

1. 安全性：检查漏洞、硬编码密钥、敏感信息泄露
2. 性能：提出优化建议
3. 可维护性：识别代码异味、冗余或复杂度过高
4. 错误处理：发现未处理异常或潜在Bug
5. 一致性：变量命名、方法命名、导入顺序等
6. 测试覆盖：列出应补充的测试用例
   请按严重程度排序，提供文件/行号级别的具体改进建议。
```

# Android TTS初始化失败问题

https://blog.csdn.net/moleft/article/details/147500384

# 支付宝 App 构建优化解析：Android 包大小极致压缩

https://mp.weixin.qq.com/s/_gnT2kjqpfMFs0kqAg4Qig

# MQTT介绍

https://www.runoob.com/w3cnote/mqtt-intro.html

# Github工具整理

https://www.runoob.com/w3cnote/github-tools.html

# AI Coding 从项目规划、分析、设计、实现、测试全流程的AI辅助流程

AI 辅助项目全流程的核心逻辑是“AI 负责数据、重复与预测，人类负责决策、边界与责任”。

AI 辅助项目全流程的核心逻辑是**“AI 负责数据、重复与预测，人类负责决策、边界与责任”**。结合 2026 年的最新实践，以下是从项目规划到测试的全流程 AI 辅助工作流拆解：

### 🚀 一、项目规划阶段：从模糊需求到结构化基线

这一阶段的核心是将“一句话需求”转化为可执行的项目蓝图。

- **需求智能拆解**：利用 NLP 技术解析非结构化需求（如会议录音、口语描述），自动提取功能点与非功能需求，生成标准化的用户故事（User Story）和验收标准。AI 还能基于历史项目数据，识别需求中的模糊点与冲突。
- **WBS 与智能排期**：AI 自动完成工作分解结构（WBS）拆解，分析任务依赖关系与关键路径。基于历史数据构建机器学习模型，AI 能输出更精准的工期与成本估算（误差率较人工降低 50% 以上），并自动生成多版本的甘特图与资源分配方案。
- **风险初判**：在立项阶段，AI 即可基于项目描述列出常见风险清单，给出预防措施，辅助管理层进行终审决策。

### 📊 二、项目分析阶段：从人工洞察到全样本智能分析

AI 将传统的“抽样分析”升级为“全样本洞察”，重塑 5W1H 分析法。

- **多维数据汇集**：整合用户行为日志、客服对话、社交媒体舆情及竞品数据等多源非结构化数据。
- **智能归因与画像**：运用思维链（CoT）推理，AI 能将复杂问题（如“用户流失率上升”）分解为子任务进行深度归因。同时，自动聚类受影响的用户群体，生成动态用户画像。
- **解决方案生成**：AI 不再仅是分析工具，而是成为“共同创造者”，基于数据支撑自动生成初步的解决方案或优化建议，供产品经理交叉验证。

### 🎨 三、项目设计阶段：从手工绘图到生成式协同

设计流程正从单点提效升级为“生成→优化→审查→交付”的自动化链路。

- **原型与 UI 生成**：通过自然语言描述或手绘草图，AI 可自动生成高保真可编辑的原型页面、线框图及交互逻辑。支持从线框图自动生成视觉样式，甚至直接进行风格迁移。
- **跨软件无缝协同**：利用 MCP 协议等数据桥梁，实现 Figma、PS、AI 绘图工具间的实时双向通信，消除手动导出导入环节。
- **设计即代码（D2C）**：AI 支持将设计稿一键转换为高质量的前端代码（如 Vue、React、HTML/CSS），实现设计与开发的无缝衔接。

### 💻 四、项目实现阶段：从手动编码到 Agentic 开发

开发模式正从 Copilot（辅助）向 Agentic（智能体自主执行）演进。

- **架构与代码生成**：AI 作为架构评审角色，提供技术选型与数据库设计建议。在编码时，采用“骨架+血肉”策略，由 AI 生成核心逻辑与样板代码，开发者聚焦于复杂业务逻辑。
- **自动化 Code Review**：AI 作为第一道防线，实时扫描代码，识别安全漏洞、N+1 查询、并发问题及命名规范，提供重构建议。
- **CI/CD 与运维**：AI 辅助生成 Dockerfile、K8s 配置及流水线脚本。在运维阶段，利用 AIOps 进行日志分析、根因定位及告警降噪。

### 🧪 五、项目测试阶段：从人工用例到智能闭环

测试环节实现了从“需求”到“缺陷闭环”的端到端自动化。

- **智能用例生成**：基于 PRD 和接口文档，AI 自动输出包含正常场景、边界条件、异常输入的测试点与用例表格，覆盖率显著提升。
- **自动化脚本编写**：AI 根据测试用例和技术栈（如 Pytest、Playwright、Postman）直接生成可运行的自动化测试脚本，并处理参数化与断言。
- **缺陷分析与回归**：测试失败后，AI 分析报错日志，自动判断是“脚本问题”还是“业务 Bug”。若是业务 Bug，自动生成包含复现步骤的缺陷描述并提交至管理系统；修复后，AI 还能辅助生成回归用例。

### 💡 核心原则与注意事项

1.  **人类锁定基线**：项目范围、关键里程碑、预算边界及核心决策必须由人类锁定，AI 负责执行与预警。
2.  **人工复核机制**：AI 生成的代码、用例及设计稿可能存在幻觉或逻辑遗漏，必须经过人工校验与本地调试。
3.  **数据安全与合规**：在使用 AI 处理项目数据时，需注意敏感数据脱敏，并确保代码与知识产权的合规性。

# 优化 AI 使用情况以最大限度地提高效率和降低成本

https://docs.github.com/zh/copilot/tutorials/optimize-ai-usage

1. 选择合适的模型

- 推理模型：最适合需要更深入分析的体系结构决策、复杂调试、系统设计和任务。
- 中层模型：最好是计划已经明确，代理需要高效执行。
- 较轻的模型：最适合重构、格式设置、文档更新和其他常规、范围良好的更改。

2. 明确任务：

- 明确的任务定义。 而不是“修复此问题”，而是解释问题所在、发生位置以及预期结果的外观。
- 预先提供的相关上下文。 如果已经知道哪些文件、服务、日志、错误或输入很重要，请包括这些文件。 这有助于智能体避免不必要的探索。
- 明确的停止条件。 明确告知智能体任务完成的判定标准。 如果没有停止点，代理可以通过添加额外的提交、重构不相关的代码或扩展范围来继续超越目标。

3. 保持上下文精简：

- 提供项目说明，减少不必要的探索
- 压缩对话，减少历史记录长度
- 不相关的任务开启新对话，避免将历史记录带入新请求

4. 缓存问题：切换对话不能复用，缓存有过期限制，大多只有1小时，切换模型会缓存失效
5. 工作分解明确阶段：

- 研究： 使用代理浏览代码库、识别相关文件以及了解依赖项。
- 计划： 在进行更改之前创建详细的结构化计划或规范。 这就是推理模型最有价值的位置-始终使用强大的推理模型进行计划，然后使用更便宜的模型实现工作。
- 实现： 使用重点上下文和适合执行的模型针对计划执行。

6. 重复实践和经验总结更新到指导说明文档里

要包括的内容：

- 所需的框架、库或设计模式
- 代理往往会重复犯的已知陷阱
- 输出预期，如“简洁”或“仅返回代码”
- 代理必须遵循的团队特定约定
- 生成、测试和 Lint 命令

要避免的事项：

- 长文档，通用文档
- AI 生成的指南不反映实际系统
- 一次性首选项或很少使用的详细信息
- 造成上下文嘈杂的过多指令

7. 防护性措施：避免小错误积累，偏离目标，提高修复成本

确定性控制机制引入了明确的通过/失败信号：

- 单元测试 验证代理的更改是否生成了预期行为。
- Linters 强制实施结构和一致性，防止格式设置问题、样式偏移和可避免的清理工作。
- 安全扫描可以及早发现风险模式，以免后续更难处理。

# Prompt提示词

https://docs.github.com/zh/copilot/concepts/prompting/prompt-engineering

1. 提供对目标或场景的概括性描述+列出具体化要求
2. 可以提供示例输入数据、示例输出和示例实现
3. 将复杂任务分解成更简单的小任务
4. 避免歧义
5. 指出相关代码范围，避免无意义搜索
6. 遵循良好的编码习惯:

- 使用一致的代码样式和模式
- 对变量和函数使用描述性名称
- 代码注释
- 功能明确的模块化组件

示例：

研究，任务，计划，执行，验证

**修复问题的提示词：**

```sh
读取 AA/BB/CC/index.vue文件

- 目前出现问题:打开报错后一直停留在loading中
- 正确情况：出现报错提示时，loading停止

请给出修复AA/BB/CC/index.vue问题的方案
```

检查方案是否符合需求，不符合继续提示修改方案，确认没问题后执行

```sh
执行上述方案，执行完成后运行命令npm run build，没有报错即为成功，有报错，请按照报错提示进行修复
```

**新增功能的提示词：**

```sh
读取 AA/BB/CC文件夹中代码

新增tts朗读功能
- 顶栏添加播放停止按钮
- 点击按钮后将当前文本内容进行tts语音朗读
- 当前文本朗读结束后按钮自动切换为停止状态

请给出tts朗读功能的实现方案
```

# 使用GitHub Copilot处理任务的最佳做法

https://docs.github.com/zh/copilot/tutorials/cloud-agent/get-the-best-results

- 清晰描述要解决的问题或所需的工作。
- 关于什么样的解决方案才算好的完整验收标准（例如，是否应该有单元测试？）。
- 关于需要更改哪些文件的指示。

你可能选择自行处理问题而非分配给 Copilot，包括：

**复杂、范围广泛的任务**

- 需要跨仓库知识和测试的范围广泛、上下文丰富的重构问题
- 需要了解依赖项和旧代码的复杂问题
- 需要深入域知识的任务
- 涉及大量业务逻辑的任务
- 对代码库的大规模更改需要保持设计一致性

**敏感和关键任务**

- 生产关键问题
- 涉及安全性、个人身份信息、身份验证反响的任务
- 事件响应

**不明确的任务**

- 缺少清晰定义的任务：要求不明确的任务、开放式任务、需要从不确定性中寻找解决方案的任务

**学习任务**

- 开发人员希望通过学习实现更深入理解的任务

# threeJs水效果

https://www.threejswaterpro.com/

# llama.cpp+本地模型+langChain如何实现联网访问搜索资料

使用 `llama.cpp`、本地模型和 `LangChain` 实现联网搜索，核心思路是为你的大语言模型（LLM）配备一个“搜索工具”。本地模型本身是离线的，无法主动访问网络，因此需要由 `LangChain` 作为框架来协调：当用户提问时，`LangChain` 先调用一个搜索工具去互联网上查找最新信息，然后将搜索到的结果和用户的原始问题组合在一起，再提交给本地模型，由模型生成最终的回答。

以下是实现这一目标的具体步骤和示例代码。

### 核心步骤

1.  **准备本地模型**：使用 `llama.cpp` 在本地运行一个大语言模型，并使其通过 API（例如通过其内置的服务器功能）对外提供服务。
2.  **在 LangChain 中配置模型**：使用 `LangChain` 的连接组件，指向你本地运行的模型 API。
3.  **配置搜索工具**：选择一个搜索工具并将其集成到 `LangChain` 中。常用选择包括：
    - **Tavily Search**：专为 AI 代理设计的搜索引擎，对开发者友好。
    - **DuckDuckGo Search**：一个免费的搜索工具，`LangChain` 有现成的集成。
    - **Bing Search / Google Search**：通过它们的官方 API 进行搜索。
4.  **创建代理（Agent）**：使用 `LangChain` 的代理功能，将本地模型和搜索工具绑定。代理具备推理能力，能自主判断何时需要调用搜索工具。

### 示例代码 (使用 Python)

下面是一个使用 `LangChain`、`llama.cpp` 本地服务器和 `DuckDuckGo` 搜索工具的完整示例。

#### 1. 安装依赖

首先，你需要安装必要的 Python 包：

```bash
pip install -U langchain langchain-community langchain-openai duckduckgo-search
pip install -U ddgs
```

#### 2. 启动 llama.cpp 本地服务器

在命令行中，使用 `llama.cpp` 启动一个本地 API 服务器。假设你已经下载了一个模型文件（例如 `model.gguf`）：

```bash
# 在 llama.cpp 的目录下运行
./server -m ./models/model.gguf -c 4096
```

这通常会在 `http://localhost:8080` 启动一个兼容 OpenAI API 格式的服务。

#### 3. 编写 Python 代码

```python
from langchain_openai import ChatOpenAI
from langchain_community.tools import DuckDuckGoSearchRun
from langgraph.prebuilt import create_react_agent

# 1. 配置本地 llama.cpp 模型
llm = ChatOpenAI(
    base_url="http://localhost:8123/v1",
    api_key="sk-xxx",          # 占位符，本地服务不需要真实 key
    model="local-model",       # 模型名称可随意填写
    temperature=0.7
)

# 2. 配置搜索工具
search_tool = DuckDuckGoSearchRun()
tools = [search_tool]

# 3. 使用 LangGraph 创建 ReAct 代理
# 注意：这里是从 langgraph.prebuilt 导入，而不是 langchain.agents
agent = create_react_agent(llm, tools)

# 4. 提问
question = "今天深圳市福田区天气怎么样？"
print(f"用户提问: {question}")

# LangGraph 使用 invoke 方法，输入格式为 messages 列表
response = agent.invoke({"messages": [("user", question)]})

# 提取最终回答（最后一条 AI 消息的内容）
final_answer = response["messages"][-1].content
print(f"最终回答: {final_answer}")
```

### 它是如何工作的？

当你运行这段代码并提出问题（例如“今天北京天气怎么样？”）时，会发生以下流程：

1.  **思考**：`LangChain` 代理接收到问题，并将其发送给本地模型。本地模型根据提示词进行思考，它会输出类似这样的内容：“我需要搜索一下北京的天气。”
2.  **行动**：代理识别出需要使用名为 `Search` 的工具，并将“北京天气”作为输入参数。
3.  **观察**：`LangChain` 框架执行 `DuckDuckGoSearchRun` 工具，获取搜索结果（例如“北京今日晴，最高气温25℃...”）。
4.  **最终回答**：代理将搜索结果和原始问题一起再次提交给本地模型。本地模型基于这些最新信息，生成一个流畅、自然语言的答案，如“根据最新信息，今天北京的天气是晴天，最高气温大约为25摄氏度。”

### 注意事项

- **模型能力**：代理的成败很大程度上取决于本地模型的指令遵循和逻辑推理能力。像 Llama 3、Qwen 2、Mistral 等较新的模型效果会更好。
- **上下文长度**：搜索结果会增加发送给模型的文本长度，请确保你的 `llama.cpp` 启动参数 `-c`（上下文长度）足够大。
- **搜索工具选择**：`DuckDuckGo` 虽然免费方便，但可能不稳定或有请求限制。对于生产环境，更推荐使用 **Tavily Search** 或官方的 **Bing/Google Search API**，它们更稳定可靠。
- **网络合规性**：在实现联网功能时，请务必遵守相关的网络安全法律法规。

# 不使用duckduckgo等第三方搜索API，单纯使用网页爬取，实现llama.cpp+本地模型+langChain联网访问搜索资料

如果不使用任何第三方搜索 API（如 DuckDuckGo、Tavily、Bing API 等），仅通过纯网页爬取实现联网搜索，核心思路是：**直接爬取搜索引擎的 HTML 结果页 -> 解析出目标链接 -> 爬取目标网页正文 -> 喂给本地模型**。

**修改逻辑，先用本地模型解析提取关键词，然后再使用搜索引擎进行关键词搜索，把搜索内容资料输入到本地模型，让其回答问题**

这种方案完全自主可控，但需要处理反爬和 HTML 解析。以下是基于 `requests` + `BeautifulSoup` + `trafilatura` + `LangChain` 的完整实现。

### 1. 安装依赖

```bash
pip install requests beautifulsoup4 trafilatura langchain-openai langchain-core lxml
```

### 2. 完整代码实现

```python
import time
import random
import requests
from bs4 import BeautifulSoup
import trafilatura
from urllib.parse import quote_plus
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate

# ================= 配置本地 llama.cpp 模型 =================
llm = ChatOpenAI(
    base_url="http://localhost:8080/v1",
    api_key="sk-xxx",
    model="local-model",
    temperature=0.1,      # 关键词提取和资料总结都需要低温度
    max_tokens=2048
)

# ================= 第一步：本地模型提取搜索关键词 =================
def extract_search_keywords(question: str) -> list[str]:
    """
    让本地模型分析用户问题，提取/改写为适合搜索引擎的关键词列表
    """
    prompt = ChatPromptTemplate.from_messages([
        ("system", """你是一个搜索查询优化专家。请将用户的自然语言问题转化为1~3个最适合搜索引擎使用的关键词或短语。
要求：
1. 去除口语化词汇、语气词和无意义的修饰语
2. 保留核心实体、时间、地点等关键信息
3. 如果问题包含多个独立子问题，拆分为多个关键词
4. 仅输出关键词，每行一个，不要任何解释、标点或编号"""),
        ("user", "{question}")
    ])

    chain = prompt | llm
    response = chain.invoke({"question": question})

    # 解析模型输出，过滤空行
    keywords = [line.strip() for line in response.content.strip().split("\n") if line.strip()]
    print(f"🔑 提取的搜索关键词: {keywords}")
    return keywords[:3]  # 最多取3个关键词


# ================= 第二步：纯网页爬取搜索引擎 =================
def scrape_search_results(query: str, num_results: int = 3) -> list[dict]:
    """爬取 Bing 搜索结果页"""
    url = f"https://www.bing.com/search?q={quote_plus(query)}&count={num_results}"
    headers = {
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
                       "(KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36",
        "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
    }

    results = []
    try:
        resp = requests.get(url, headers=headers, timeout=15)
        resp.raise_for_status()
        soup = BeautifulSoup(resp.text, "lxml")
        items = soup.select("li.b_algo")

        for item in items[:num_results]:
            a_tag = item.select_one("h2 a")
            snippet_tag = item.select_one(".b_caption p")
            if a_tag and a_tag.get("href"):
                results.append({
                    "title": a_tag.get_text(strip=True),
                    "url": a_tag["href"],
                    "snippet": snippet_tag.get_text(strip=True) if snippet_tag else ""
                })
    except Exception as e:
        print(f"❌ 搜索爬取失败 ({query}): {e}")

    return results


def scrape_page_content(url: str, max_chars: int = 2000) -> str:
    """爬取网页正文"""
    try:
        downloaded = trafilatura.fetch_url(url)
        if not downloaded:
            return ""
        text = trafilatura.extract(downloaded, include_comments=False, include_tables=True)
        return text[:max_chars] if text else ""
    except Exception:
        return ""


def search_with_keywords(keywords: list[str], results_per_keyword: int = 2) -> str:
    """用提取的关键词逐个搜索，合并去重后返回上下文"""
    all_contexts = []
    seen_urls = set()
    idx = 0

    for kw in keywords:
        print(f"\n🔍 正在搜索关键词: {kw}")
        results = scrape_search_results(kw, num_results=results_per_keyword)

        for result in results:
            if result["url"] in seen_urls:
                continue
            seen_urls.add(result["url"])
            idx += 1

            print(f"  📄 [{idx}] 提取: {result['title']}")
            content = scrape_page_content(result["url"])
            if not content:
                content = result["snippet"]

            if content:
                all_contexts.append(f"--- 资料{idx}: {result['title']} ---\n{content}")

        # ⚠️ 反爬保护：每次搜索间随机延迟
        time.sleep(random.uniform(2, 4))

    return "\n\n".join(all_contexts)


# ================= 第三步：基于资料回答问题 =================
def answer_with_context(question: str, context: str) -> str:
    prompt = ChatPromptTemplate.from_messages([
        ("system", """你是一个严谨的AI助手。请**仅根据**以下【参考资料】回答用户问题。
如果资料中没有相关信息，请明确告知"提供的资料中未找到相关答案"，禁止编造。
回答时请注明信息来源（如"根据资料1..."）。

【参考资料】:
{context}"""),
        ("user", "{question}")
    ])

    chain = prompt | llm
    response = chain.invoke({"context": context, "question": question})
    return response.content


# ================= 主流程 =================
if __name__ == "__main__":
    user_question = "最近SpaceX星舰第七次试飞成功了吗？有什么新的技术突破？"

    print(f"💬 用户提问: {user_question}\n")

    # Step 1: 本地模型提取关键词
    keywords = extract_search_keywords(user_question)

    if not keywords:
        print("⚠️ 未能提取有效关键词，使用原始问题搜索")
        keywords = [user_question]

    # Step 2: 用关键词搜索并爬取资料
    context = search_with_keywords(keywords, results_per_keyword=2)

    if not context:
        print("\n未获取到任何有效资料，请检查网络或更换关键词。")
    else:
        # Step 3: 本地模型基于资料回答
        print("\n🤖 模型正在基于爬取资料生成回答...\n")
        answer = answer_with_context(user_question, context)
        print("=" * 50)
        print(answer)
```

### 3. 关键技术点说明

| 环节      | 技术选型                 | 原因                                                                     |
| :-------- | :----------------------- | :----------------------------------------------------------------------- |
| 搜索引擎  | Bing HTML                | Google 反爬极严，百度结果质量差；Bing 结构稳定、中文支持好、反爬阈值较高 |
| HTML 解析 | BeautifulSoup + lxml     | 比正则可靠，`lxml` 解析速度远快于默认解析器                              |
| 正文提取  | trafilatura              | 基于机器学习的主内容提取，自动过滤导航/广告/侧栏，输出干净文本           |
| 反爬对抗  | 真实 UA + Accept 头      | 模拟正常浏览器请求，避免被直接拒绝                                       |
| 容错降级  | 全文爬取失败时用搜索摘要 | 保证即使目标网站反爬，仍有基础信息可用                                   |

### 4. ⚠️ 风险与应对策略

纯网页爬取方案相比 API 方案有明显脆弱性，必须做好以下防护：

- **IP 封禁风险**：高频请求必然被封。务必加入 `time.sleep(random.uniform(2, 5))` 随机延迟；如需长期使用，应接入代理池轮换 IP。
- **HTML 结构变更**：搜索引擎会不定期改版导致选择器失效。建议将 CSS 选择器抽为配置项，并增加异常告警，便于快速修复。
- **法律合规**：仅用于个人学习研究，不得用于商业产品；遵守目标网站的 `robots.txt`；不对服务器造成过大负载。
- **内容质量不稳定**：爬取的网页可能包含无关内容或乱码。`trafilatura` 已能过滤大部分噪声，但建议在 Prompt 中强调"忽略资料中与问题无关的内容"，让模型自行甄别。
- **编码问题**：部分中文网站使用 GBK 编码，`requests` 自动检测可能出错。可在 `scrape_page_content` 中加入编码兜底：
  ```python
  resp.encoding = resp.apparent_encoding  # 强制自动检测编码
  ```

### 5. 何时应该放弃纯爬虫转向 API

如果你遇到以下情况，建议回归搜索 API 方案：

- 需要稳定的生产级服务（纯爬虫无法保证 SLA）
- 搜索频率 > 10次/分钟
- 需要结构化搜索结果（如知识图谱、实时股价等）
- 目标搜索引擎持续升级反爬，维护成本超过 API 费用

纯爬虫方案最适合**个人本地实验、低频查询、以及不想注册任何第三方服务的场景**。
